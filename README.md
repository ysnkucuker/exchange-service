# Exchange Service 💱

A simple **Foreign Exchange Service** built with **Spring Boot** and **PostgreSQL**, using [Frankfurter API](https://www.frankfurter.dev) for currency rates.

## 🚀 Features
- **Exchange Rate API** → Get exchange rate between two currencies
- **Conversion API** → Convert an amount from one currency to another and store transaction
- **Conversion List API** → Retrieve conversions by transaction ID or date
- **Centralized Error Handling** → Custom `ApiException` and global exception handler
- **Database Integration** → PostgreSQL with JPA/Hibernate
- **Docker Support** → Runs easily with `docker-compose`

## 📂 Project Structure
